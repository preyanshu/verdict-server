{
  "name": "verdict-contracts-hardhat",
  "version": "1.0.0",
  "description": "Verdict Prediction Market Contracts - Hardhat Deployment",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network mantleTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "ethers": "^6.14.0"
  }
}

